# SeeStory core
Flask==3.0.3

# EPUB + document parsing
ebooklib==0.18
beautifulsoup4==4.12.3
lxml==5.2.2
pdfplumber==0.11.4
python-docx==1.1.2

# Images / preview frame / format conversion
Pillow==10.4.0

# Local image generation
# PyTorch is installed separately by install_all.bat from the CUDA 12.8 index.
diffusers>=0.35,<0.40
transformers>=4.48,<5
accelerate>=1.2,<2
safetensors>=0.5,<1

# ffmpeg is required for motion clips and final MP4 assembly.
# It is a system program, not a pip package. install_all.bat checks for it.
